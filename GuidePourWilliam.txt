Code Région du rapport

avec departement du praticien et table departement où le code Région est indiqué


SELECT * 
FROM rapport_visite 
WHERE SUBSTR(praticien.PRA_CP, 1 , 2) = departement.REG_CODE = "PL";

début de requête pour sql pour récupérer les rapports d'une région données, en l'occurence PL